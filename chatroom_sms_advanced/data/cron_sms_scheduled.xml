<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron: Process Scheduled SMS (every 5 minutes) -->
        <record id="cron_process_scheduled_sms" model="ir.cron">
            <field name="name">SMS Advanced: Process Scheduled SMS</field>
            <field name="model_id" ref="model_sms_scheduled"/>
            <field name="state">code</field>
            <field name="code">model.cron_process_scheduled_sms()</field>
            <field name="interval_number">5</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <field name="priority">5</field>
        </record>

        <!-- Cron: Check Provider Balance (every 6 hours) -->
        <record id="cron_check_provider_balance" model="ir.cron">
            <field name="name">SMS Advanced: Check Provider Balance</field>
            <field name="model_id" ref="sms_base_sr.model_sms_provider"/>
            <field name="state">code</field>
            <field name="code">model.cron_check_balance()</field>
            <field name="interval_number">6</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <field name="priority">10</field>
        </record>

        <!-- Cron: Sync Blacklist to Kolmeya (every 1 hour) -->
        <record id="cron_sync_blacklist" model="ir.cron">
            <field name="name">SMS Advanced: Sync Blacklist to Provider</field>
            <field name="model_id" ref="model_sms_blacklist"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_blacklist()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <field name="priority">15</field>
        </record>

    </data>
</odoo>
